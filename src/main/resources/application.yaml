server:
  port : 8081

spring:
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: testmailappwise@gmail.com
    password: testingwise
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false

  datasource:
    driver-class-name: org.postgresql.Driver
